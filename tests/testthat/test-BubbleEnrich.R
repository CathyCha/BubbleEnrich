context("Bubble GSEA")
library(BubbleGSEA)
library("testthat")

test_that("the function output is a ggplot", {
  # data(geneList, package = "DOSE")
  # bubble_graph <- BubbleEnrich(geneList)
  # expect_is(bubble_graph,"ggplot")
})
